/** The internal profile tags an admin may toggle (the owner, 2/9) —
 *  admin-only member_crm data, never shown to members or clients.
 *  Shared by the server action (validation) and the UI (chips). */
export const MEMBER_INTERNAL_TAGS = [
  "מפתחת AI ללקוח אמיתי",
  "מפתחת AI לפרויקטים עצמאיים",
  "פיתוח מודלים",
] as const;
