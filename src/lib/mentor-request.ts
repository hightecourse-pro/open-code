/**
 * The subject the profile-page mentor-request card files under. Shared
 * between the action that writes it and the page that checks for an open one
 * ("use server" modules may only export async functions, so it lives here).
 */
export const MENTOR_REQUEST_SUBJECT = "בקשה להצטרף כמנטורית";
